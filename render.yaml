services:
  - type: web
    name: mincha-bot
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: ID_INSTANCE
        sync: false
      - key: API_TOKEN
        sync: false
      - key: CHAT_ID
        sync: false
